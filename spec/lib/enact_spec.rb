require "spec_helper"

describe "enact" do
  describe "directive :my_method => :resulting_delta do |arg1, arg2|" do
    it "applies :resulting_delta and returns it in a delta set" do
    end
  end
end
